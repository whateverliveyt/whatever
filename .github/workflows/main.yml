name: Video Rotation Stage 2

on:
  workflow_dispatch:

jobs:
  rotation-test:
    runs-on: ubuntu-latest

    steps:
            - name: Simulasi rotasi harian (FIX)
        run: |
          echo "=== SEBELUM ROTASI ==="
          echo "ListA:"
          ls listA_*.mp4
          echo "ListB:"
          ls listB_*.mp4

          KELUAR=$(ls listA_*.mp4 | shuf -n 1)
          MASUK=$(ls listB_*.mp4 | shuf -n 1)

          echo ""
          echo "Video KELUAR dari listA: $KELUAR"
          echo "Video MASUK dari listB:  $MASUK"

          mv "$KELUAR" keluar.mp4
          mv "$MASUK" masuk.mp4

          mv masuk.mp4 "$KELUAR"
          mv keluar.mp4 "$MASUK"

          echo ""
          echo "=== SETELAH ROTASI ==="
          echo "ListA:"
          ls listA_*.mp4
          echo "ListB:"
          ls listB_*.mp4

